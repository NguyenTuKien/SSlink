apiVersion: v1
kind: Secret
metadata:
  name: '{{ include "helm.fullname" . }}-secret'
  labels:
    {{- include "helm.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.opencv.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
